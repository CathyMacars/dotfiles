[user]
	name = Catarina
	email = c.macars@gmail.com

[alias]
	a = add
	ad = add
	b = branch
	br = branch
	ch = checkout
	cl = clone
	cm = commit
	d = difftool
	df = difftool
	dt = difftool
	li = list
	lg = log --graph --pretty=format:'%C(yellow)[%h] %C(cyan)%an %Cgreen(%cr) %C(reset)→ %s'
	pl = pull
	ps = push
	r = rebase
	rb = rebase
	sh = stash
	sm = submodule
	st = status

[push]
	default = simple

[github]
	user = CathyMacars

[credential]
	helper = osxkeychain

[difftool "Kaleidoscope"]
	cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[diff]
	tool = Kaleidoscope
[difftool]
	prompt = false

[mergetool "Kaleidoscope"]
	cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
	trustExitCode = true
[mergetool]
	prompt = false
[merge]
	tool = Kaleidoscope